<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="teamrenaissance.foodbasket" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:theme="@style/AppTheme" >

        <!-- android:icon="@drawable/app_icon" -->
        <!-- android:label="@string/app_name" -->


        <!-- <activity -->
        <!-- android:name=".toDelete.ItemListActivity" -->
        <!-- android:label="@string/app_name" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".toDelete.ItemDetailActivity" -->
        <!-- android:label="@string/title_item_detail" -->
        <!-- android:parentActivityName=".toDelete.ItemListActivity" > -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".toDelete.ItemListActivity" /> -->
        <!-- </activity> -->
        <activity
            android:name=".user.ScannerActivity"
            android:label="@string/title_activity_scanner" >
        </activity>
        <activity
            android:name=".admin.LoginRegisterActivity"
            android:icon="@drawable/app_icon"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".toDelete.RegisterActivity" -->
        <!-- android:label="@string/title_activity_register" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".toDelete.AddItemActivity" -->
        <!-- android:label="@string/title_activity_add_item" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".toDelete.EditItemActivity" -->
        <!-- android:label="@string/title_activity_edit_item" > -->
        <!-- </activity> -->
        <activity
            android:name=".user.CreateEntryActivity"
            android:label="@string/title_activity_create_entry" >
        </activity>
        <activity
            android:name=".user.DetailedEntryActivity"
            android:label="@string/title_activity_detailed_entry" >
        </activity>
        <activity
            android:name=".user.EditEntryActivity"
            android:label="@string/title_activity_edit_entry" >
        </activity>
        <activity
            android:name=".user.InventoryListActivity"
            android:label="@string/title_activity_inventory_list" >
        </activity>
        <activity
            android:name=".admin.TabHostActivity"
            android:label="@string/title_activity_tab_host" >
        </activity>
        <activity
            android:name=".user.RecipeListActivity"
            android:label="@string/title_activity_recipe_list" >
        </activity>
        <activity
            android:name=".user.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name=".user.ProductInfoActivity"
            android:label="@string/title_activity_product_info" >
        </activity>
    </application>

</manifest>
